spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_api_first
    username: postgres
    password: SUA_SENHA_AQUI

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true